# 何をしてるの
- StoryBoard上の任意の位置にUILabelを配置し、プログラムからUILabelに「Hello　World」という値を設定する。
- StoryBoard上の任意の位置に、UILabelとUIButtonを配置し、ボタンを押すごとにUILabelのテキストカラーをランダムに変更する。

# テキストカラーを変える時
RGBで色の指定をする時、0.0~1.0の間で色指定が必要。
num/255することで、0~255の範囲が使えるようになる。

# 環境
Swift 4.2
Xcode 10